<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
            <%= link_to 'Admin dashboard', root_path, class: 'nav-link text-light' %>
        </li>
        <% if session['id'] %>
          <li class="nav-item">
            <%= form_with method: 'post', url: user_logout_path do |form| %>
              <button class="nav-link text-light" type="submit">Logout</button>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item">
              <%= link_to 'User register', user_register_path, class: 'nav-link text-light' %>
          </li>
          <li class="nav-item">
              <%= link_to 'User login', user_login_path, class: 'nav-link text-light' %>
          </li>
        <% end %>
    </div>
  </div>
</nav>